# Packages this retrieval + chat orchestration slice needs.
# Merge into your existing requirements.txt — don't overwrite it,
# since it likely already pins versions for other parts of the app.

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
openai>=1.30
qdrant-client>=1.9
redis>=5.0
numpy>=1.26
tiktoken>=0.7
pytest>=8.0
pytest-asyncio>=0.23